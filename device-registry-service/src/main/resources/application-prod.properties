# Produkcioni profil (aktivira se sa SPRING_PROFILES_ACTIVE=prod).
# Hostovi i kredencijali dolaze iz okruzenja (docker-compose.prod.yml + .env).

# Ne diraj semu automatski u produkciji - samo validiraj da odgovara entitetima.
spring.jpa.hibernate.ddl-auto=validate

# Bez ispisa SQL upita u produkciji.
spring.jpa.show-sql=false
